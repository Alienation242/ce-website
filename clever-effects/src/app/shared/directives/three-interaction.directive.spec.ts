import { ThreeInteractionDirective } from './three-interaction.directive';

describe('ThreeInteractionDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreeInteractionDirective();
    expect(directive).toBeTruthy();
  });
});
